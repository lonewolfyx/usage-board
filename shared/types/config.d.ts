export interface IRuntimeConfig {
    appVersion: string
    home: string
}

export interface IConfig {
    version: string
    home: string
    claudeCodePath: string
    claudeCodePaths: string[]
    codexPath: string
    geminiPath: string
}
